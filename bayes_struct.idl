
#ifndef IDL_BAYES_STRUCT
#define IDL_BAYES_STRUCT

module bayes_opt {

  /* ---------- Constants ---------- */

  const unsigned long MAX_PARAMS = 32;
  const unsigned long STATUS_LENGTH = 128;


  /* ---------- Typedefs ---------- */

  struct result {
    double value;
    boolean valid;
  };

  struct control {
    boolean allow;
  };

  struct suggestion {
    double params[MAX_PARAMS];
    long iteration;
  };

  struct best {
    double params[MAX_PARAMS];
    double value;
  };

  struct status {
    char text[STATUS_LENGTH];
    long iteration;
    boolean running;
  };


  /* ---------- Component state ---------- */

  struct state {

    /* optimization status */
    boolean running;
    boolean initialized;

    /* optimization progress */
    long current_iteration;
    long max_iterations;

    /* current returned score */
    double current_score;

    /* best result found */
    double best_value;

    /* parameter vectors */
    param_array current_params;
    best_param_array best_params;

    double lower_bounds[5];
    double upper_bounds[5];
  };

};

#endif /* IDL_BAYES_STRUCT */
