/* --- Generated by genom 3.1. Do not edit ------------------------------ */

#include "autoconf/acheader.h"

#include <assert.h>
#include <errno.h>
#include <inttypes.h>
#include <limits.h>
#include <math.h>
#include <stdarg.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "h2errorLib.h"

#include "bayes_opt/c/client.h"
#include "bayes_opt_json-types.h"
#include "private.h"


/* --- Service abort_activity encoding/decoding ------------------------- */

int
genom_bayes_opt_client_abort_activity_json_scan(
  struct genom_bayes_opt_abort_activity_input *in, const char *json,
  char **endptr)
{
  int s = 0;
  json_skip_whitespace(json);
  if (*(json++) != '{') { s = EINVAL; goto done; }
  json_skip_whitespace(json);
  while(*json != '}') {
    if (*(json++) != '"') { s = EINVAL; goto done; };
    if (!strncmp(json, "activity\"", 1+8)) {
      json += 1+8;
      json_skip_whitespace(json);
      if (*(json++) != ':') { s = EINVAL; goto done; }
      if ((s = json_scan_unsigned_long(
             &(in->activity), &json)))
        goto done;
    } else
    { s = EINVAL; goto done; }

    json_skip_whitespace(json);
    if (*json == ',') {
      json++;
      json_skip_whitespace(json);
    }
  }

done:
  if (endptr) *endptr = (char *)json;
  return s;
}

int
genom_bayes_opt_client_abort_activity_json_print(char **json,
             const struct genom_bayes_opt_abort_activity_output *out)
{
  size_t len;
  char *end;
  int s;
  (void)out; /* fix -Wunused-parameter */

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;


  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}


/* --- Service connect_port encoding/decoding --------------------------- */

int
genom_bayes_opt_client_connect_port_json_scan(
  struct genom_bayes_opt_connect_port_input *in, const char *json,
  char **endptr)
{
  int s = 0;
  json_skip_whitespace(json);
  if (*(json++) != '{') { s = EINVAL; goto done; }
  json_skip_whitespace(json);
  while(*json != '}') {
    if (*(json++) != '"') { s = EINVAL; goto done; };
    if (!strncmp(json, "local\"", 1+5)) {
      json += 1+5;
      json_skip_whitespace(json);
      if (*(json++) != ':') { s = EINVAL; goto done; }
      if ((s = json_scan_string128(
             in->local, &json)))
        goto done;
    } else
    if (!strncmp(json, "remote\"", 1+6)) {
      json += 1+6;
      json_skip_whitespace(json);
      if (*(json++) != ':') { s = EINVAL; goto done; }
      if ((s = json_scan_string128(
             in->remote, &json)))
        goto done;
    } else
    { s = EINVAL; goto done; }

    json_skip_whitespace(json);
    if (*json == ',') {
      json++;
      json_skip_whitespace(json);
    }
  }

done:
  if (endptr) *endptr = (char *)json;
  return s;
}

int
genom_bayes_opt_client_connect_port_json_print(char **json,
             const struct genom_bayes_opt_connect_port_output *out)
{
  size_t len;
  char *end;
  int s;
  (void)out; /* fix -Wunused-parameter */

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;


  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}


/* --- Service connect_service encoding/decoding ------------------------ */

int
genom_bayes_opt_client_connect_service_json_scan(
  struct genom_bayes_opt_connect_service_input *in, const char *json,
  char **endptr)
{
  int s = 0;
  json_skip_whitespace(json);
  if (*(json++) != '{') { s = EINVAL; goto done; }
  json_skip_whitespace(json);
  while(*json != '}') {
    if (*(json++) != '"') { s = EINVAL; goto done; };
    if (!strncmp(json, "local\"", 1+5)) {
      json += 1+5;
      json_skip_whitespace(json);
      if (*(json++) != ':') { s = EINVAL; goto done; }
      if ((s = json_scan_string128(
             in->local, &json)))
        goto done;
    } else
    if (!strncmp(json, "remote\"", 1+6)) {
      json += 1+6;
      json_skip_whitespace(json);
      if (*(json++) != ':') { s = EINVAL; goto done; }
      if ((s = json_scan_string128(
             in->remote, &json)))
        goto done;
    } else
    { s = EINVAL; goto done; }

    json_skip_whitespace(json);
    if (*json == ',') {
      json++;
      json_skip_whitespace(json);
    }
  }

done:
  if (endptr) *endptr = (char *)json;
  return s;
}

int
genom_bayes_opt_client_connect_service_json_print(char **json,
             const struct genom_bayes_opt_connect_service_output *out)
{
  size_t len;
  char *end;
  int s;
  (void)out; /* fix -Wunused-parameter */

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;


  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}


/* --- Service kill encoding/decoding ----------------------------------- */

int
genom_bayes_opt_client_kill_json_scan(
  struct genom_bayes_opt_kill_input *in, const char *json,
  char **endptr)
{
  int s = 0;
  (void)in; /* fix -Wunused-parameter */
  json_skip_whitespace(json);
  if (*(json++) != '{') { s = EINVAL; goto done; }
  json_skip_whitespace(json);
  while(*json != '}') {
    if (*(json++) != '"') { s = EINVAL; goto done; };
    { s = EINVAL; goto done; }

    json_skip_whitespace(json);
    if (*json == ',') {
      json++;
      json_skip_whitespace(json);
    }
  }

done:
  if (endptr) *endptr = (char *)json;
  return s;
}

int
genom_bayes_opt_client_kill_json_print(char **json,
             const struct genom_bayes_opt_kill_output *out)
{
  size_t len;
  char *end;
  int s;
  (void)out; /* fix -Wunused-parameter */

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;


  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}


/* --- Service reset_optimizer encoding/decoding ------------------------ */

int
genom_bayes_opt_client_reset_optimizer_json_scan(
  struct genom_bayes_opt_reset_optimizer_input *in, const char *json,
  char **endptr)
{
  int s = 0;
  (void)in; /* fix -Wunused-parameter */
  json_skip_whitespace(json);
  if (*(json++) != '{') { s = EINVAL; goto done; }
  json_skip_whitespace(json);
  while(*json != '}') {
    if (*(json++) != '"') { s = EINVAL; goto done; };
    { s = EINVAL; goto done; }

    json_skip_whitespace(json);
    if (*json == ',') {
      json++;
      json_skip_whitespace(json);
    }
  }

done:
  if (endptr) *endptr = (char *)json;
  return s;
}

int
genom_bayes_opt_client_reset_optimizer_json_print(char **json,
             const struct genom_bayes_opt_reset_optimizer_output *out)
{
  size_t len;
  char *end;
  int s;
  (void)out; /* fix -Wunused-parameter */

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;


  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}


/* --- Service Init encoding/decoding ----------------------------------- */

int
genom_bayes_opt_client_Init_json_scan(
  struct genom_bayes_opt_Init_input *in, const char *json,
  char **endptr)
{
  int s = 0;
  json_skip_whitespace(json);
  if (*(json++) != '{') { s = EINVAL; goto done; }
  json_skip_whitespace(json);
  while(*json != '}') {
    if (*(json++) != '"') { s = EINVAL; goto done; };
    if (!strncmp(json, "lower_bounds\"", 1+12)) {
      json += 1+12;
      json_skip_whitespace(json);
      if (*(json++) != ':') { s = EINVAL; goto done; }
      if ((s = json_scan_array5_double(
             in->lower_bounds, &json)))
        goto done;
    } else
    if (!strncmp(json, "upper_bounds\"", 1+12)) {
      json += 1+12;
      json_skip_whitespace(json);
      if (*(json++) != ':') { s = EINVAL; goto done; }
      if ((s = json_scan_array5_double(
             in->upper_bounds, &json)))
        goto done;
    } else
    if (!strncmp(json, "max_iterations\"", 1+14)) {
      json += 1+14;
      json_skip_whitespace(json);
      if (*(json++) != ':') { s = EINVAL; goto done; }
      if ((s = json_scan_long(
             &(in->max_iterations), &json)))
        goto done;
    } else
    if (!strncmp(json, "reference_x\"", 1+11)) {
      json += 1+11;
      json_skip_whitespace(json);
      if (*(json++) != ':') { s = EINVAL; goto done; }
      if ((s = json_scan_double(
             &(in->reference_x), &json)))
        goto done;
    } else
    if (!strncmp(json, "reference_y\"", 1+11)) {
      json += 1+11;
      json_skip_whitespace(json);
      if (*(json++) != ':') { s = EINVAL; goto done; }
      if ((s = json_scan_double(
             &(in->reference_y), &json)))
        goto done;
    } else
    if (!strncmp(json, "reference_z\"", 1+11)) {
      json += 1+11;
      json_skip_whitespace(json);
      if (*(json++) != ':') { s = EINVAL; goto done; }
      if ((s = json_scan_double(
             &(in->reference_z), &json)))
        goto done;
    } else
    { s = EINVAL; goto done; }

    json_skip_whitespace(json);
    if (*json == ',') {
      json++;
      json_skip_whitespace(json);
    }
  }

done:
  if (endptr) *endptr = (char *)json;
  return s;
}

int
genom_bayes_opt_client_Init_json_print(char **json,
             const struct genom_bayes_opt_Init_output *out)
{
  size_t len;
  char *end;
  int s;
  (void)out; /* fix -Wunused-parameter */

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;


  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}


/* --- Service AskNext encoding/decoding -------------------------------- */

int
genom_bayes_opt_client_AskNext_json_scan(
  struct genom_bayes_opt_AskNext_input *in, const char *json,
  char **endptr)
{
  int s = 0;
  (void)in; /* fix -Wunused-parameter */
  json_skip_whitespace(json);
  if (*(json++) != '{') { s = EINVAL; goto done; }
  json_skip_whitespace(json);
  while(*json != '}') {
    if (*(json++) != '"') { s = EINVAL; goto done; };
    { s = EINVAL; goto done; }

    json_skip_whitespace(json);
    if (*json == ',') {
      json++;
      json_skip_whitespace(json);
    }
  }

done:
  if (endptr) *endptr = (char *)json;
  return s;
}

int
genom_bayes_opt_client_AskNext_json_print(char **json,
             const struct genom_bayes_opt_AskNext_output *out)
{
  size_t len;
  char *end;
  int s;

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;

  if ((s = bufcat(json, &end, &len, 0, "\"params_out\":")))
    goto done;
  if ((s = json_print_t_bayes_opt_suggestion(json, &end, &len,
         &(out->params_out))))
    goto done;

  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}


/* --- Service UpdateFromMeasure encoding/decoding ---------------------- */

int
genom_bayes_opt_client_UpdateFromMeasure_json_scan(
  struct genom_bayes_opt_UpdateFromMeasure_input *in, const char *json,
  char **endptr)
{
  int s = 0;
  (void)in; /* fix -Wunused-parameter */
  json_skip_whitespace(json);
  if (*(json++) != '{') { s = EINVAL; goto done; }
  json_skip_whitespace(json);
  while(*json != '}') {
    if (*(json++) != '"') { s = EINVAL; goto done; };
    { s = EINVAL; goto done; }

    json_skip_whitespace(json);
    if (*json == ',') {
      json++;
      json_skip_whitespace(json);
    }
  }

done:
  if (endptr) *endptr = (char *)json;
  return s;
}

int
genom_bayes_opt_client_UpdateFromMeasure_json_print(char **json,
             const struct genom_bayes_opt_UpdateFromMeasure_output *out)
{
  size_t len;
  char *end;
  int s;
  (void)out; /* fix -Wunused-parameter */

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;


  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}


/* --- Service GetBest encoding/decoding -------------------------------- */

int
genom_bayes_opt_client_GetBest_json_scan(
  struct genom_bayes_opt_GetBest_input *in, const char *json,
  char **endptr)
{
  int s = 0;
  (void)in; /* fix -Wunused-parameter */
  json_skip_whitespace(json);
  if (*(json++) != '{') { s = EINVAL; goto done; }
  json_skip_whitespace(json);
  while(*json != '}') {
    if (*(json++) != '"') { s = EINVAL; goto done; };
    { s = EINVAL; goto done; }

    json_skip_whitespace(json);
    if (*json == ',') {
      json++;
      json_skip_whitespace(json);
    }
  }

done:
  if (endptr) *endptr = (char *)json;
  return s;
}

int
genom_bayes_opt_client_GetBest_json_print(char **json,
             const struct genom_bayes_opt_GetBest_output *out)
{
  size_t len;
  char *end;
  int s;

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;

  if ((s = bufcat(json, &end, &len, 0, "\"best_result_out\":")))
    goto done;
  if ((s = json_print_t_bayes_opt_best(json, &end, &len,
         &(out->best_result_out))))
    goto done;

  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}


/* --- Service Reset encoding/decoding ---------------------------------- */

int
genom_bayes_opt_client_Reset_json_scan(
  struct genom_bayes_opt_Reset_input *in, const char *json,
  char **endptr)
{
  int s = 0;
  (void)in; /* fix -Wunused-parameter */
  json_skip_whitespace(json);
  if (*(json++) != '{') { s = EINVAL; goto done; }
  json_skip_whitespace(json);
  while(*json != '}') {
    if (*(json++) != '"') { s = EINVAL; goto done; };
    { s = EINVAL; goto done; }

    json_skip_whitespace(json);
    if (*json == ',') {
      json++;
      json_skip_whitespace(json);
    }
  }

done:
  if (endptr) *endptr = (char *)json;
  return s;
}

int
genom_bayes_opt_client_Reset_json_print(char **json,
             const struct genom_bayes_opt_Reset_output *out)
{
  size_t len;
  char *end;
  int s;
  (void)out; /* fix -Wunused-parameter */

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;


  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}



/* --- Port genom_state encoding ---------------------------------------- */


int
genom_bayes_opt_client_genom_state_json_print(char **json,
  const genom_state_component *data)
{
  size_t len;
  char *end;
  int s;

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;

  if ((s = bufcat(json, &end, &len, 0, "\"genom_state\":")))
    goto done;

  if ((s = json_print_t_genom_state_component(
         json, &end, &len, &(*(data)))))
    goto done;

  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}


/* --- Port measure encoding -------------------------------------------- */

int
genom_bayes_opt_client_measure_json_scan(
  or_pose_estimator_state *data,
  const char *json, char **endptr)
{
  int s = 0;

  json_skip_whitespace(json);
  if (*(json++) != '{') { s = EINVAL; goto done; }
  json_skip_whitespace(json);
  while(*json != '}') {
    if (*(json++) != '"') { s = EINVAL; goto done; };

    if (strncmp(json, "measure\"", 1+7)) {
      s = EINVAL; goto done;
    }

    json += 1+7;
    json_skip_whitespace(json);
    if (*(json++) != ':') { s = EINVAL; goto done; }
    if ((s = json_scan_t_or_pose_estimator_state(data, &json)))
      goto done;

    json_skip_whitespace(json);
    if (*json == ',') {
      json++;
      json_skip_whitespace(json);
    }
  }

done:
  if (endptr) *endptr = (char *)json;
  return s;
}

int
genom_bayes_opt_client_measure_json_print(char **json,
  const or_pose_estimator_state *data)
{
  size_t len;
  char *end;
  int s;

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;

  if ((s = bufcat(json, &end, &len, 0, "\"measure\":")))
    goto done;

  if ((s = json_print_t_or_pose_estimator_state(
         json, &end, &len, &(*(data)))))
    goto done;

  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}


/* --- Port allow encoding ---------------------------------------------- */

int
genom_bayes_opt_client_allow_json_scan(
  bayes_opt_control *data,
  const char *json, char **endptr)
{
  int s = 0;

  json_skip_whitespace(json);
  if (*(json++) != '{') { s = EINVAL; goto done; }
  json_skip_whitespace(json);
  while(*json != '}') {
    if (*(json++) != '"') { s = EINVAL; goto done; };

    if (strncmp(json, "allow\"", 1+5)) {
      s = EINVAL; goto done;
    }

    json += 1+5;
    json_skip_whitespace(json);
    if (*(json++) != ':') { s = EINVAL; goto done; }
    if ((s = json_scan_t_bayes_opt_control(data, &json)))
      goto done;

    json_skip_whitespace(json);
    if (*json == ',') {
      json++;
      json_skip_whitespace(json);
    }
  }

done:
  if (endptr) *endptr = (char *)json;
  return s;
}

int
genom_bayes_opt_client_allow_json_print(char **json,
  const bayes_opt_control *data)
{
  size_t len;
  char *end;
  int s;

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;

  if ((s = bufcat(json, &end, &len, 0, "\"allow\":")))
    goto done;

  if ((s = json_print_t_bayes_opt_control(
         json, &end, &len, &(*(data)))))
    goto done;

  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}


/* --- Port params encoding --------------------------------------------- */


int
genom_bayes_opt_client_params_json_print(char **json,
  const bayes_opt_suggestion *data)
{
  size_t len;
  char *end;
  int s;

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;

  if ((s = bufcat(json, &end, &len, 0, "\"params\":")))
    goto done;

  if ((s = json_print_t_bayes_opt_suggestion(
         json, &end, &len, &(*(data)))))
    goto done;

  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}


/* --- Port best_result encoding ---------------------------------------- */


int
genom_bayes_opt_client_best_result_json_print(char **json,
  const bayes_opt_best *data)
{
  size_t len;
  char *end;
  int s;

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;

  if ((s = bufcat(json, &end, &len, 0, "\"best_result\":")))
    goto done;

  if ((s = json_print_t_bayes_opt_best(
         json, &end, &len, &(*(data)))))
    goto done;

  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}


/* --- Port status encoding --------------------------------------------- */


int
genom_bayes_opt_client_status_json_print(char **json,
  const bayes_opt_status_struct *data)
{
  size_t len;
  char *end;
  int s;

  len = JSON_MINBUF;
  *json = end = malloc(len);
  if (!*json) return ENOMEM;

  if ((s = bufcat(json, &end, &len, 0, "{"))) goto done;

  if ((s = bufcat(json, &end, &len, 0, "\"status\":")))
    goto done;

  if ((s = json_print_t_bayes_opt_status_struct(
         json, &end, &len, &(*(data)))))
    goto done;

  if ((s = bufcat(json, &end, &len, 0, "}"))) goto done;

done:
  if (s && *json) {
    free(*json);
    *json = NULL;
  }
  return s;
}


/* --- bayes_opt_json_error --------------------------------------------- */

char *
genom_bayes_opt_client_json_error(genom_client h,
                         genom_event e, const void *detail)
{
  char *s, *end;
  size_t len;

  len = JSON_MINBUF;
  s = end = malloc(len);
  if (!s) return NULL;

  if (!e && h) detail = h->context.raised(&e, &h->context);

  if (e == genom_syserr_id) {
    if (bufcat(&s, &end, &len, 0, "\"")) return NULL;
    if (bufcat(&s, &end, &len, 1,
               strerror(((genom_syserr_detail *)detail)->code))) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"")) return NULL;
    return s;
  } else if (e == genom_unkex_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_unkex_id)) return NULL;
    if (detail) {
      if (bufcat(&s, &end, &len, 0, "\",\"detail\":{\"what\":\"")) return NULL;
      if (bufcat(&s, &end, &len, 1,
                 ((genom_unkex_detail *)detail)->what)) return NULL;
    }
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    if (detail)
      if (bufcat(&s, &end, &len, 0, "}")) return NULL;
    return s;
  } else if (e == genom_fatal) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_fatal)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }
  else if (e == genom_incompatible_digest_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_incompatible_digest_id)) return NULL;
    if (detail) {
      if (bufcat(&s, &end, &len, 0, "\",\"detail\":")) return NULL;
      if (json_print_t_genom_incompatible_digest(&s, &end, &len, detail)) return NULL;
    }
    if (bufcat(&s, &end, &len, 0, "}")) return NULL;
    return s;
  }
  else if (e == genom_bad_transition_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_bad_transition_id)) return NULL;
    if (detail) {
      if (bufcat(&s, &end, &len, 0, "\",\"detail\":")) return NULL;
      if (json_print_t_genom_bad_transition(&s, &end, &len, detail)) return NULL;
    }
    if (bufcat(&s, &end, &len, 0, "}")) return NULL;
    return s;
  }
  else if (e == genom_interrupted_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_interrupted_id)) return NULL;
    if (detail) {
      if (bufcat(&s, &end, &len, 0, "\",\"detail\":")) return NULL;
      if (json_print_t_genom_interrupted(&s, &end, &len, detail)) return NULL;
    }
    if (bufcat(&s, &end, &len, 0, "}")) return NULL;
    return s;
  }
  else if (e == genom_serialization_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_serialization_id)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }
  else if (e == genom_too_many_activities_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_too_many_activities_id)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }
  else if (e == genom_disallowed_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_disallowed_id)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }
  else if (e == genom_mwerr_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_mwerr_id)) return NULL;
    if (detail) {
      if (bufcat(&s, &end, &len, 0, "\",\"detail\":")) return NULL;
      if (json_print_t_genom_mwerr(&s, &end, &len, detail)) return NULL;
    }
    if (bufcat(&s, &end, &len, 0, "}")) return NULL;
    return s;
  }
  else if (e == genom_no_such_activity_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_no_such_activity_id)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }
  else if (e == genom_no_such_inport_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_no_such_inport_id)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }
  else if (e == genom_no_such_outport_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_no_such_outport_id)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }
  else if (e == genom_port_io_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_port_io_id)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }
  else if (e == genom_no_such_remote_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_no_such_remote_id)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }
  else if (e == genom_no_such_service_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_no_such_service_id)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }
  else if (e == genom_remote_io_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, genom_remote_io_id)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }
  else if (e == bayes_opt_INVALID_BOUNDS_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, bayes_opt_INVALID_BOUNDS_id)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }
  else if (e == bayes_opt_e_sys_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, bayes_opt_e_sys_id)) return NULL;
    if (detail) {
      if (bufcat(&s, &end, &len, 0, "\",\"detail\":")) return NULL;
      if (json_print_t_bayes_opt_e_sys(&s, &end, &len, detail)) return NULL;
    }
    if (bufcat(&s, &end, &len, 0, "}")) return NULL;
    return s;
  }
  else if (e == bayes_opt_NOT_INITIALIZED_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, bayes_opt_NOT_INITIALIZED_id)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }
  else if (e == bayes_opt_OPTIMIZATION_FAILED_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, bayes_opt_OPTIMIZATION_FAILED_id)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }
  else if (e == bayes_opt_NO_MEASUREMENT_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, bayes_opt_NO_MEASUREMENT_id)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }
  else if (e == bayes_opt_NO_BEST_RESULT_id) {
    if (bufcat(&s, &end, &len, 0, "{\"ex\":\"")) return NULL;
    if (bufcat(&s, &end, &len, 0, bayes_opt_NO_BEST_RESULT_id)) return NULL;
    if (bufcat(&s, &end, &len, 0, "\"}")) return NULL;
    return s;
  }

  assert(!"unknown genom exception");
  return NULL;
}
