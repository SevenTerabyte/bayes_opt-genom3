/* --- Generated by genom 3.1. Do not edit ------------------------------ */

#include "autoconf/acheader.h"

#include "portLib.h"
#include "errnoLib.h"
#include "gcomLib.h"

#include "bayes_opt/c/client.h"
#include "private.h"
#include "serialize.h"
#include "typecopy.h"
#include "bayes_opt_msglib.h"


/* --- Service abort_activity encoding/decoding ------------------------- */

int
genom_bayes_opt_abort_activity_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  char *p = dst;
  struct genom_bayes_opt_abort_activity_input *in =
    (struct genom_bayes_opt_abort_activity_input *)buffer;
  ssize_t s;
  (void)size; /* fix -Wunused-parameter */

  s = 0;
  s += genom_serialen_unsigned_long(
    in->activity);
  if (s > maxsize) return ERROR;

  genom_serialize_unsigned_long(
    &p, in->activity);

  return s;
}

int
genom_bayes_opt_abort_activity_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_no_such_activity_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_no_such_activity_id);
    a->report = genom_no_such_activity_id;
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_bayes_opt_client_abort_activity_init_input(
  struct genom_bayes_opt_abort_activity_input *in)
{
  genom_tinit_unsigned_long(
    &(in->activity));

  /* set default value */
  genom_bayes_opt_client_abort_activity_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_bayes_opt_client_abort_activity_init_output(
  struct genom_bayes_opt_abort_activity_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_bayes_opt_client_abort_activity_fini_input(
  struct genom_bayes_opt_abort_activity_input *in)
{
  genom_tfini_unsigned_long(
    &(in->activity));
}

void
genom_bayes_opt_client_abort_activity_fini_output(
  struct genom_bayes_opt_abort_activity_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}


/* --- Service connect_port encoding/decoding --------------------------- */

int
genom_bayes_opt_connect_port_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  char *p = dst;
  struct genom_bayes_opt_connect_port_input *in =
    (struct genom_bayes_opt_connect_port_input *)buffer;
  ssize_t s;
  (void)size; /* fix -Wunused-parameter */

  s = 0;
  s += genom_serialen_string128(
    in->local);
  s += genom_serialen_string128(
    in->remote);
  if (s > maxsize) return ERROR;

  genom_serialize_string128(
    &p, in->local);
  genom_serialize_string128(
    &p, in->remote);

  return s;
}

int
genom_bayes_opt_connect_port_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_no_such_inport_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_no_such_inport_id);
    a->report = genom_no_such_inport_id;
  } else if (!strcmp(p, genom_no_such_outport_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_no_such_outport_id);
    a->report = genom_no_such_outport_id;
  } else if (!strcmp(p, genom_port_io_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_port_io_id);
    a->report = genom_port_io_id;
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_bayes_opt_client_connect_port_init_input(
  struct genom_bayes_opt_connect_port_input *in)
{
  genom_tinit_string128(
    in->local);
  genom_tinit_string128(
    in->remote);

  /* set default value */
  genom_bayes_opt_client_connect_port_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_bayes_opt_client_connect_port_init_output(
  struct genom_bayes_opt_connect_port_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_bayes_opt_client_connect_port_fini_input(
  struct genom_bayes_opt_connect_port_input *in)
{
  genom_tfini_string128(
    in->local);
  genom_tfini_string128(
    in->remote);
}

void
genom_bayes_opt_client_connect_port_fini_output(
  struct genom_bayes_opt_connect_port_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}


/* --- Service connect_service encoding/decoding ------------------------ */

int
genom_bayes_opt_connect_service_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  char *p = dst;
  struct genom_bayes_opt_connect_service_input *in =
    (struct genom_bayes_opt_connect_service_input *)buffer;
  ssize_t s;
  (void)size; /* fix -Wunused-parameter */

  s = 0;
  s += genom_serialen_string128(
    in->local);
  s += genom_serialen_string128(
    in->remote);
  if (s > maxsize) return ERROR;

  genom_serialize_string128(
    &p, in->local);
  genom_serialize_string128(
    &p, in->remote);

  return s;
}

int
genom_bayes_opt_connect_service_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_no_such_remote_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_no_such_remote_id);
    a->report = genom_no_such_remote_id;
  } else if (!strcmp(p, genom_no_such_service_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_no_such_service_id);
    a->report = genom_no_such_service_id;
  } else if (!strcmp(p, genom_remote_io_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_remote_io_id);
    a->report = genom_remote_io_id;
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_bayes_opt_client_connect_service_init_input(
  struct genom_bayes_opt_connect_service_input *in)
{
  genom_tinit_string128(
    in->local);
  genom_tinit_string128(
    in->remote);

  /* set default value */
  genom_bayes_opt_client_connect_service_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_bayes_opt_client_connect_service_init_output(
  struct genom_bayes_opt_connect_service_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_bayes_opt_client_connect_service_fini_input(
  struct genom_bayes_opt_connect_service_input *in)
{
  genom_tfini_string128(
    in->local);
  genom_tfini_string128(
    in->remote);
}

void
genom_bayes_opt_client_connect_service_fini_output(
  struct genom_bayes_opt_connect_service_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}


/* --- Service kill encoding/decoding ----------------------------------- */

int
genom_bayes_opt_kill_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_bayes_opt_kill_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_bayes_opt_client_kill_init_input(
  struct genom_bayes_opt_kill_input *in)
{

  /* set default value */
  genom_bayes_opt_client_kill_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_bayes_opt_client_kill_init_output(
  struct genom_bayes_opt_kill_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_bayes_opt_client_kill_fini_input(
  struct genom_bayes_opt_kill_input *in)
{
  (void)in; /* fix -Wunused-parameter */
}

void
genom_bayes_opt_client_kill_fini_output(
  struct genom_bayes_opt_kill_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}


/* --- Service reset_optimizer encoding/decoding ------------------------ */

int
genom_bayes_opt_reset_optimizer_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_bayes_opt_reset_optimizer_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_bayes_opt_client_reset_optimizer_init_input(
  struct genom_bayes_opt_reset_optimizer_input *in)
{

  /* set default value */
  genom_bayes_opt_client_reset_optimizer_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_bayes_opt_client_reset_optimizer_init_output(
  struct genom_bayes_opt_reset_optimizer_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_bayes_opt_client_reset_optimizer_fini_input(
  struct genom_bayes_opt_reset_optimizer_input *in)
{
  (void)in; /* fix -Wunused-parameter */
}

void
genom_bayes_opt_client_reset_optimizer_fini_output(
  struct genom_bayes_opt_reset_optimizer_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}


/* --- Service Init encoding/decoding ----------------------------------- */

int
genom_bayes_opt_Init_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  char *p = dst;
  struct genom_bayes_opt_Init_input *in =
    (struct genom_bayes_opt_Init_input *)buffer;
  ssize_t s;
  (void)size; /* fix -Wunused-parameter */

  s = 0;
  s += genom_serialen_array5_double(
    in->lower_bounds);
  s += genom_serialen_array5_double(
    in->upper_bounds);
  s += genom_serialen_long(
    in->max_iterations);
  s += genom_serialen_double(
    in->reference_x);
  s += genom_serialen_double(
    in->reference_y);
  s += genom_serialen_double(
    in->reference_z);
  s += genom_serialen_double(
    in->reference_qw);
  s += genom_serialen_double(
    in->reference_qx);
  s += genom_serialen_double(
    in->reference_qy);
  s += genom_serialen_double(
    in->reference_qz);
  if (s > maxsize) return ERROR;

  genom_serialize_array5_double(
    &p, in->lower_bounds);
  genom_serialize_array5_double(
    &p, in->upper_bounds);
  genom_serialize_long(
    &p, in->max_iterations);
  genom_serialize_double(
    &p, in->reference_x);
  genom_serialize_double(
    &p, in->reference_y);
  genom_serialize_double(
    &p, in->reference_z);
  genom_serialize_double(
    &p, in->reference_qw);
  genom_serialize_double(
    &p, in->reference_qx);
  genom_serialize_double(
    &p, in->reference_qy);
  genom_serialize_double(
    &p, in->reference_qz);

  return s;
}

int
genom_bayes_opt_Init_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, bayes_opt_INVALID_BOUNDS_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(bayes_opt_INVALID_BOUNDS_id);
    a->report = bayes_opt_INVALID_BOUNDS_id;
  } else if (!strcmp(p, bayes_opt_e_sys_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(bayes_opt_e_sys_id);
    a->report = bayes_opt_e_sys_id;
    p += sizeof(bayes_opt_e_sys_id);
    a->exdetail = malloc(sizeof(bayes_opt_e_sys_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_bayes_opt_e_sys(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_bayes_opt_e_sys;
    s = genom_deserialize_t_bayes_opt_e_sys(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_bayes_opt_client_Init_init_input(
  struct genom_bayes_opt_Init_input *in)
{
  genom_tinit_array5_double(
    in->lower_bounds);
  genom_tinit_array5_double(
    in->upper_bounds);
  genom_tinit_long(
    &(in->max_iterations));
  genom_tinit_double(
    &(in->reference_x));
  genom_tinit_double(
    &(in->reference_y));
  genom_tinit_double(
    &(in->reference_z));
  genom_tinit_double(
    &(in->reference_qw));
  genom_tinit_double(
    &(in->reference_qx));
  genom_tinit_double(
    &(in->reference_qy));
  genom_tinit_double(
    &(in->reference_qz));

  /* set default value */
  genom_bayes_opt_client_Init_json_scan(in,
                  "{ \"reference_x\":0,\"reference_y\":0,\"reference_z\":1,\"reference_qw\":1,\"reference_qx\":0,\"reference_qy\":0,\"reference_qz\":0 }",
                  NULL);
}

void
genom_bayes_opt_client_Init_init_output(
  struct genom_bayes_opt_Init_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_bayes_opt_client_Init_fini_input(
  struct genom_bayes_opt_Init_input *in)
{
  genom_tfini_array5_double(
    in->lower_bounds);
  genom_tfini_array5_double(
    in->upper_bounds);
  genom_tfini_long(
    &(in->max_iterations));
  genom_tfini_double(
    &(in->reference_x));
  genom_tfini_double(
    &(in->reference_y));
  genom_tfini_double(
    &(in->reference_z));
  genom_tfini_double(
    &(in->reference_qw));
  genom_tfini_double(
    &(in->reference_qx));
  genom_tfini_double(
    &(in->reference_qy));
  genom_tfini_double(
    &(in->reference_qz));
}

void
genom_bayes_opt_client_Init_fini_output(
  struct genom_bayes_opt_Init_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}


/* --- Service AskNext encoding/decoding -------------------------------- */

int
genom_bayes_opt_AskNext_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_bayes_opt_AskNext_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  struct genom_bayes_opt_AskNext_output *out = a->output;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;
    s = genom_deserialize_t_bayes_opt_suggestion(
      &p, &len, &(out->params_out));

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, bayes_opt_NOT_INITIALIZED_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(bayes_opt_NOT_INITIALIZED_id);
    a->report = bayes_opt_NOT_INITIALIZED_id;
  } else if (!strcmp(p, bayes_opt_OPTIMIZATION_FAILED_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(bayes_opt_OPTIMIZATION_FAILED_id);
    a->report = bayes_opt_OPTIMIZATION_FAILED_id;
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_bayes_opt_client_AskNext_init_input(
  struct genom_bayes_opt_AskNext_input *in)
{

  /* set default value */
  genom_bayes_opt_client_AskNext_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_bayes_opt_client_AskNext_init_output(
  struct genom_bayes_opt_AskNext_output *out)
{
  genom_tinit_t_bayes_opt_suggestion(
    &(out->params_out));
}

void
genom_bayes_opt_client_AskNext_fini_input(
  struct genom_bayes_opt_AskNext_input *in)
{
  (void)in; /* fix -Wunused-parameter */
}

void
genom_bayes_opt_client_AskNext_fini_output(
  struct genom_bayes_opt_AskNext_output *out)
{
  genom_tfini_t_bayes_opt_suggestion(
    &(out->params_out));
}


/* --- Service UpdateFromMeasure encoding/decoding ---------------------- */

int
genom_bayes_opt_UpdateFromMeasure_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_bayes_opt_UpdateFromMeasure_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, bayes_opt_NOT_INITIALIZED_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(bayes_opt_NOT_INITIALIZED_id);
    a->report = bayes_opt_NOT_INITIALIZED_id;
  } else if (!strcmp(p, bayes_opt_NO_MEASUREMENT_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(bayes_opt_NO_MEASUREMENT_id);
    a->report = bayes_opt_NO_MEASUREMENT_id;
  } else if (!strcmp(p, bayes_opt_OPTIMIZATION_FAILED_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(bayes_opt_OPTIMIZATION_FAILED_id);
    a->report = bayes_opt_OPTIMIZATION_FAILED_id;
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_bayes_opt_client_UpdateFromMeasure_init_input(
  struct genom_bayes_opt_UpdateFromMeasure_input *in)
{

  /* set default value */
  genom_bayes_opt_client_UpdateFromMeasure_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_bayes_opt_client_UpdateFromMeasure_init_output(
  struct genom_bayes_opt_UpdateFromMeasure_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_bayes_opt_client_UpdateFromMeasure_fini_input(
  struct genom_bayes_opt_UpdateFromMeasure_input *in)
{
  (void)in; /* fix -Wunused-parameter */
}

void
genom_bayes_opt_client_UpdateFromMeasure_fini_output(
  struct genom_bayes_opt_UpdateFromMeasure_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}


/* --- Service GetBest encoding/decoding -------------------------------- */

int
genom_bayes_opt_GetBest_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_bayes_opt_GetBest_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  struct genom_bayes_opt_GetBest_output *out = a->output;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;
    s = genom_deserialize_t_bayes_opt_best(
      &p, &len, &(out->best_result_out));

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, bayes_opt_NOT_INITIALIZED_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(bayes_opt_NOT_INITIALIZED_id);
    a->report = bayes_opt_NOT_INITIALIZED_id;
  } else if (!strcmp(p, bayes_opt_NO_BEST_RESULT_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(bayes_opt_NO_BEST_RESULT_id);
    a->report = bayes_opt_NO_BEST_RESULT_id;
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_bayes_opt_client_GetBest_init_input(
  struct genom_bayes_opt_GetBest_input *in)
{

  /* set default value */
  genom_bayes_opt_client_GetBest_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_bayes_opt_client_GetBest_init_output(
  struct genom_bayes_opt_GetBest_output *out)
{
  genom_tinit_t_bayes_opt_best(
    &(out->best_result_out));
}

void
genom_bayes_opt_client_GetBest_fini_input(
  struct genom_bayes_opt_GetBest_input *in)
{
  (void)in; /* fix -Wunused-parameter */
}

void
genom_bayes_opt_client_GetBest_fini_output(
  struct genom_bayes_opt_GetBest_output *out)
{
  genom_tfini_t_bayes_opt_best(
    &(out->best_result_out));
}


/* --- Service Reset encoding/decoding ---------------------------------- */

int
genom_bayes_opt_Reset_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_bayes_opt_Reset_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, bayes_opt_e_sys_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(bayes_opt_e_sys_id);
    a->report = bayes_opt_e_sys_id;
    p += sizeof(bayes_opt_e_sys_id);
    a->exdetail = malloc(sizeof(bayes_opt_e_sys_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_bayes_opt_e_sys(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_bayes_opt_e_sys;
    s = genom_deserialize_t_bayes_opt_e_sys(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_bayes_opt_client_Reset_init_input(
  struct genom_bayes_opt_Reset_input *in)
{

  /* set default value */
  genom_bayes_opt_client_Reset_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_bayes_opt_client_Reset_init_output(
  struct genom_bayes_opt_Reset_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_bayes_opt_client_Reset_fini_input(
  struct genom_bayes_opt_Reset_input *in)
{
  (void)in; /* fix -Wunused-parameter */
}

void
genom_bayes_opt_client_Reset_fini_output(
  struct genom_bayes_opt_Reset_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

