/* --- Generated by genom 3.1. Do not edit ------------------------------ */

#ifndef H_BAYES_OPT_MSGLIB
#define H_BAYES_OPT_MSGLIB

#include <stddef.h>

#include "portLib.h"
#include "csLib.h"

#include "serialize.h"


/* --- Service/task id ----------------------------------------------------- */

enum {
  BAYES_OPT_abort_activity_RQSTID,
  BAYES_OPT_connect_port_RQSTID,
  BAYES_OPT_connect_service_RQSTID,
  BAYES_OPT_kill_RQSTID,
  BAYES_OPT_reset_optimizer_RQSTID,
  BAYES_OPT_Init_RQSTID,
  BAYES_OPT_AskNext_RQSTID,
  BAYES_OPT_UpdateFromMeasure_RQSTID,
  BAYES_OPT_GetBest_RQSTID,
  BAYES_OPT_Reset_RQSTID,

  /* total */
  BAYES_OPT_NRQSTID
};

enum {
  bayes_opt_optimize_TASKID,

  /* total */
  bayes_opt_NTASKID
};


/* --- Mailboxes ----------------------------------------------------------- */

/* mailbox buffer size for a message of size "size" */
#define MBOX_BUF_SIZE(size)                             \
  ((size) + 4 - ((size) & 3) + sizeof(LETTER_HDR) + 8)

/* Maximum request size - compilers should optimize this away */
static __inline__ size_t
genom_bayes_opt_max_rqst_size(void)
{
  size_t m = 0, s;

  /* input of abort_activity */
  s = 0;
  s += genom_maxserialen_unsigned_long();
  if (m < s) m = s;

  /* input of connect_port */
  s = 0;
  s += genom_maxserialen_string128();
  s += genom_maxserialen_string128();
  if (m < s) m = s;

  /* input of connect_service */
  s = 0;
  s += genom_maxserialen_string128();
  s += genom_maxserialen_string128();
  if (m < s) m = s;

  /* input of kill */
  s = 0;
  if (m < s) m = s;

  /* input of reset_optimizer */
  s = 0;
  if (m < s) m = s;

  /* input of Init */
  s = 0;
  s += genom_maxserialen_array5_double();
  s += genom_maxserialen_array5_double();
  s += genom_maxserialen_long();
  s += genom_maxserialen_double();
  s += genom_maxserialen_double();
  s += genom_maxserialen_double();
  if (m < s) m = s;

  /* input of AskNext */
  s = 0;
  if (m < s) m = s;

  /* input of UpdateFromMeasure */
  s = 0;
  if (m < s) m = s;

  /* input of GetBest */
  s = 0;
  if (m < s) m = s;

  /* input of Reset */
  s = 0;
  if (m < s) m = s;

  return m;
}
#define  BAYES_OPT_MAX_RQST_SIZE	genom_bayes_opt_max_rqst_size()

/* Size of reception mailbox */
#define  BAYES_OPT_MBOX_RQST_SIZE                                \
  (MBOX_BUF_SIZE(BAYES_OPT_MAX_RQST_SIZE) * SERV_NMAX_RQST_ID)


/* Maximum reply size */
static __inline__ size_t
genom_bayes_opt_max_reply_size()
{
  size_t m = 0, s;

  /* output of abort_activity */
  s = 1; /* ok flag */
  if (m < s) m = s;

  /* throws of abort_activity */
  s = sizeof(genom_incompatible_digest_id);
  s += genom_maxserialen_t_genom_incompatible_digest();
  if (m < s) m = s;
  s = sizeof(genom_bad_transition_id);
  s += genom_maxserialen_t_genom_bad_transition();
  if (m < s) m = s;
  s = sizeof(genom_interrupted_id);
  s += genom_maxserialen_t_genom_interrupted();
  if (m < s) m = s;
  s = sizeof(genom_serialization_id);
  s += genom_maxserialen_t_genom_serialization();
  if (m < s) m = s;
  s = sizeof(genom_too_many_activities_id);
  s += genom_maxserialen_t_genom_too_many_activities();
  if (m < s) m = s;
  s = sizeof(genom_disallowed_id);
  s += genom_maxserialen_t_genom_disallowed();
  if (m < s) m = s;
  s = sizeof(genom_mwerr_id);
  s += genom_maxserialen_t_genom_mwerr();
  if (m < s) m = s;
  s = sizeof(genom_no_such_activity_id);
  s += genom_maxserialen_t_genom_no_such_activity();
  if (m < s) m = s;

  /* output of connect_port */
  s = 1; /* ok flag */
  if (m < s) m = s;

  /* throws of connect_port */
  s = sizeof(genom_incompatible_digest_id);
  s += genom_maxserialen_t_genom_incompatible_digest();
  if (m < s) m = s;
  s = sizeof(genom_bad_transition_id);
  s += genom_maxserialen_t_genom_bad_transition();
  if (m < s) m = s;
  s = sizeof(genom_interrupted_id);
  s += genom_maxserialen_t_genom_interrupted();
  if (m < s) m = s;
  s = sizeof(genom_serialization_id);
  s += genom_maxserialen_t_genom_serialization();
  if (m < s) m = s;
  s = sizeof(genom_too_many_activities_id);
  s += genom_maxserialen_t_genom_too_many_activities();
  if (m < s) m = s;
  s = sizeof(genom_disallowed_id);
  s += genom_maxserialen_t_genom_disallowed();
  if (m < s) m = s;
  s = sizeof(genom_mwerr_id);
  s += genom_maxserialen_t_genom_mwerr();
  if (m < s) m = s;
  s = sizeof(genom_no_such_inport_id);
  s += genom_maxserialen_t_genom_no_such_inport();
  if (m < s) m = s;
  s = sizeof(genom_no_such_outport_id);
  s += genom_maxserialen_t_genom_no_such_outport();
  if (m < s) m = s;
  s = sizeof(genom_port_io_id);
  s += genom_maxserialen_t_genom_port_io();
  if (m < s) m = s;

  /* output of connect_service */
  s = 1; /* ok flag */
  if (m < s) m = s;

  /* throws of connect_service */
  s = sizeof(genom_incompatible_digest_id);
  s += genom_maxserialen_t_genom_incompatible_digest();
  if (m < s) m = s;
  s = sizeof(genom_bad_transition_id);
  s += genom_maxserialen_t_genom_bad_transition();
  if (m < s) m = s;
  s = sizeof(genom_interrupted_id);
  s += genom_maxserialen_t_genom_interrupted();
  if (m < s) m = s;
  s = sizeof(genom_serialization_id);
  s += genom_maxserialen_t_genom_serialization();
  if (m < s) m = s;
  s = sizeof(genom_too_many_activities_id);
  s += genom_maxserialen_t_genom_too_many_activities();
  if (m < s) m = s;
  s = sizeof(genom_disallowed_id);
  s += genom_maxserialen_t_genom_disallowed();
  if (m < s) m = s;
  s = sizeof(genom_mwerr_id);
  s += genom_maxserialen_t_genom_mwerr();
  if (m < s) m = s;
  s = sizeof(genom_no_such_remote_id);
  s += genom_maxserialen_t_genom_no_such_remote();
  if (m < s) m = s;
  s = sizeof(genom_no_such_service_id);
  s += genom_maxserialen_t_genom_no_such_service();
  if (m < s) m = s;
  s = sizeof(genom_remote_io_id);
  s += genom_maxserialen_t_genom_remote_io();
  if (m < s) m = s;

  /* output of kill */
  s = 1; /* ok flag */
  if (m < s) m = s;

  /* throws of kill */
  s = sizeof(genom_incompatible_digest_id);
  s += genom_maxserialen_t_genom_incompatible_digest();
  if (m < s) m = s;
  s = sizeof(genom_bad_transition_id);
  s += genom_maxserialen_t_genom_bad_transition();
  if (m < s) m = s;
  s = sizeof(genom_interrupted_id);
  s += genom_maxserialen_t_genom_interrupted();
  if (m < s) m = s;
  s = sizeof(genom_serialization_id);
  s += genom_maxserialen_t_genom_serialization();
  if (m < s) m = s;
  s = sizeof(genom_too_many_activities_id);
  s += genom_maxserialen_t_genom_too_many_activities();
  if (m < s) m = s;
  s = sizeof(genom_disallowed_id);
  s += genom_maxserialen_t_genom_disallowed();
  if (m < s) m = s;
  s = sizeof(genom_mwerr_id);
  s += genom_maxserialen_t_genom_mwerr();
  if (m < s) m = s;

  /* output of reset_optimizer */
  s = 1; /* ok flag */
  if (m < s) m = s;

  /* throws of reset_optimizer */
  s = sizeof(genom_incompatible_digest_id);
  s += genom_maxserialen_t_genom_incompatible_digest();
  if (m < s) m = s;
  s = sizeof(genom_bad_transition_id);
  s += genom_maxserialen_t_genom_bad_transition();
  if (m < s) m = s;
  s = sizeof(genom_interrupted_id);
  s += genom_maxserialen_t_genom_interrupted();
  if (m < s) m = s;
  s = sizeof(genom_serialization_id);
  s += genom_maxserialen_t_genom_serialization();
  if (m < s) m = s;
  s = sizeof(genom_too_many_activities_id);
  s += genom_maxserialen_t_genom_too_many_activities();
  if (m < s) m = s;
  s = sizeof(genom_disallowed_id);
  s += genom_maxserialen_t_genom_disallowed();
  if (m < s) m = s;
  s = sizeof(genom_mwerr_id);
  s += genom_maxserialen_t_genom_mwerr();
  if (m < s) m = s;

  /* output of Init */
  s = 1; /* ok flag */
  if (m < s) m = s;

  /* throws of Init */
  s = sizeof(genom_incompatible_digest_id);
  s += genom_maxserialen_t_genom_incompatible_digest();
  if (m < s) m = s;
  s = sizeof(genom_bad_transition_id);
  s += genom_maxserialen_t_genom_bad_transition();
  if (m < s) m = s;
  s = sizeof(genom_interrupted_id);
  s += genom_maxserialen_t_genom_interrupted();
  if (m < s) m = s;
  s = sizeof(genom_serialization_id);
  s += genom_maxserialen_t_genom_serialization();
  if (m < s) m = s;
  s = sizeof(genom_too_many_activities_id);
  s += genom_maxserialen_t_genom_too_many_activities();
  if (m < s) m = s;
  s = sizeof(genom_disallowed_id);
  s += genom_maxserialen_t_genom_disallowed();
  if (m < s) m = s;
  s = sizeof(genom_mwerr_id);
  s += genom_maxserialen_t_genom_mwerr();
  if (m < s) m = s;
  s = sizeof(bayes_opt_INVALID_BOUNDS_id);
  s += genom_maxserialen_t_bayes_opt_INVALID_BOUNDS();
  if (m < s) m = s;
  s = sizeof(bayes_opt_e_sys_id);
  s += genom_maxserialen_t_bayes_opt_e_sys();
  if (m < s) m = s;

  /* output of AskNext */
  s = 1; /* ok flag */
  s += genom_maxserialen_t_bayes_opt_suggestion();
  if (m < s) m = s;

  /* throws of AskNext */
  s = sizeof(genom_incompatible_digest_id);
  s += genom_maxserialen_t_genom_incompatible_digest();
  if (m < s) m = s;
  s = sizeof(genom_bad_transition_id);
  s += genom_maxserialen_t_genom_bad_transition();
  if (m < s) m = s;
  s = sizeof(genom_interrupted_id);
  s += genom_maxserialen_t_genom_interrupted();
  if (m < s) m = s;
  s = sizeof(genom_serialization_id);
  s += genom_maxserialen_t_genom_serialization();
  if (m < s) m = s;
  s = sizeof(genom_too_many_activities_id);
  s += genom_maxserialen_t_genom_too_many_activities();
  if (m < s) m = s;
  s = sizeof(genom_disallowed_id);
  s += genom_maxserialen_t_genom_disallowed();
  if (m < s) m = s;
  s = sizeof(genom_mwerr_id);
  s += genom_maxserialen_t_genom_mwerr();
  if (m < s) m = s;
  s = sizeof(bayes_opt_NOT_INITIALIZED_id);
  s += genom_maxserialen_t_bayes_opt_NOT_INITIALIZED();
  if (m < s) m = s;
  s = sizeof(bayes_opt_OPTIMIZATION_FAILED_id);
  s += genom_maxserialen_t_bayes_opt_OPTIMIZATION_FAILED();
  if (m < s) m = s;

  /* output of UpdateFromMeasure */
  s = 1; /* ok flag */
  if (m < s) m = s;

  /* throws of UpdateFromMeasure */
  s = sizeof(genom_incompatible_digest_id);
  s += genom_maxserialen_t_genom_incompatible_digest();
  if (m < s) m = s;
  s = sizeof(genom_bad_transition_id);
  s += genom_maxserialen_t_genom_bad_transition();
  if (m < s) m = s;
  s = sizeof(genom_interrupted_id);
  s += genom_maxserialen_t_genom_interrupted();
  if (m < s) m = s;
  s = sizeof(genom_serialization_id);
  s += genom_maxserialen_t_genom_serialization();
  if (m < s) m = s;
  s = sizeof(genom_too_many_activities_id);
  s += genom_maxserialen_t_genom_too_many_activities();
  if (m < s) m = s;
  s = sizeof(genom_disallowed_id);
  s += genom_maxserialen_t_genom_disallowed();
  if (m < s) m = s;
  s = sizeof(genom_mwerr_id);
  s += genom_maxserialen_t_genom_mwerr();
  if (m < s) m = s;
  s = sizeof(bayes_opt_NOT_INITIALIZED_id);
  s += genom_maxserialen_t_bayes_opt_NOT_INITIALIZED();
  if (m < s) m = s;
  s = sizeof(bayes_opt_NO_MEASUREMENT_id);
  s += genom_maxserialen_t_bayes_opt_NO_MEASUREMENT();
  if (m < s) m = s;
  s = sizeof(bayes_opt_OPTIMIZATION_FAILED_id);
  s += genom_maxserialen_t_bayes_opt_OPTIMIZATION_FAILED();
  if (m < s) m = s;

  /* output of GetBest */
  s = 1; /* ok flag */
  s += genom_maxserialen_t_bayes_opt_best();
  if (m < s) m = s;

  /* throws of GetBest */
  s = sizeof(genom_incompatible_digest_id);
  s += genom_maxserialen_t_genom_incompatible_digest();
  if (m < s) m = s;
  s = sizeof(genom_bad_transition_id);
  s += genom_maxserialen_t_genom_bad_transition();
  if (m < s) m = s;
  s = sizeof(genom_interrupted_id);
  s += genom_maxserialen_t_genom_interrupted();
  if (m < s) m = s;
  s = sizeof(genom_serialization_id);
  s += genom_maxserialen_t_genom_serialization();
  if (m < s) m = s;
  s = sizeof(genom_too_many_activities_id);
  s += genom_maxserialen_t_genom_too_many_activities();
  if (m < s) m = s;
  s = sizeof(genom_disallowed_id);
  s += genom_maxserialen_t_genom_disallowed();
  if (m < s) m = s;
  s = sizeof(genom_mwerr_id);
  s += genom_maxserialen_t_genom_mwerr();
  if (m < s) m = s;
  s = sizeof(bayes_opt_NOT_INITIALIZED_id);
  s += genom_maxserialen_t_bayes_opt_NOT_INITIALIZED();
  if (m < s) m = s;
  s = sizeof(bayes_opt_NO_BEST_RESULT_id);
  s += genom_maxserialen_t_bayes_opt_NO_BEST_RESULT();
  if (m < s) m = s;

  /* output of Reset */
  s = 1; /* ok flag */
  if (m < s) m = s;

  /* throws of Reset */
  s = sizeof(genom_incompatible_digest_id);
  s += genom_maxserialen_t_genom_incompatible_digest();
  if (m < s) m = s;
  s = sizeof(genom_bad_transition_id);
  s += genom_maxserialen_t_genom_bad_transition();
  if (m < s) m = s;
  s = sizeof(genom_interrupted_id);
  s += genom_maxserialen_t_genom_interrupted();
  if (m < s) m = s;
  s = sizeof(genom_serialization_id);
  s += genom_maxserialen_t_genom_serialization();
  if (m < s) m = s;
  s = sizeof(genom_too_many_activities_id);
  s += genom_maxserialen_t_genom_too_many_activities();
  if (m < s) m = s;
  s = sizeof(genom_disallowed_id);
  s += genom_maxserialen_t_genom_disallowed();
  if (m < s) m = s;
  s = sizeof(genom_mwerr_id);
  s += genom_maxserialen_t_genom_mwerr();
  if (m < s) m = s;
  s = sizeof(bayes_opt_e_sys_id);
  s += genom_maxserialen_t_bayes_opt_e_sys();
  if (m < s) m = s;


  s = sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
  if (m < s) m = s;
  s = sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
  if (m < s) m = s;
  return m;
}
#define  BAYES_OPT_MAX_REPLY_SIZE		genom_bayes_opt_max_reply_size()
#define  BAYES_OPT_MAX_INTERMED_REPLY_SIZE	sizeof(int)

/* Size of reply mbox (clients) */
#define  BAYES_OPT_CLIENT_MBOX_REPLY_SIZE        	\
  ((MBOX_BUF_SIZE(BAYES_OPT_MAX_REPLY_SIZE) +		\
    MBOX_BUF_SIZE(BAYES_OPT_MAX_INTERMED_REPLY_SIZE))	\
   * CLIENT_NMAX_RQST_ID)

#endif /* H_BAYES_OPT_MSGLIB */
