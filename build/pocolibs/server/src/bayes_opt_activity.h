/* --- Generated by genom 3.1. Do not edit ------------------------------ */

#ifndef H_BAYES_OPT_ACTIVITY
#define H_BAYES_OPT_ACTIVITY

#include "typecopy.h"
#include "bayes_opt_parameters.h"

/* forward declaration */
struct genom_component_data;

enum genom_activity_status {
  ACT_VOID,	/* nothing */
  ACT_INIT,	/* in control task codels */
  ACT_RUN,	/* managed by exec task, running */
  ACT_STOP,	/* managed by exec task, stopping */
  ACT_ETHER	/* result available for control task */
};

struct genom_activity {
  int sid;	/* service number, -1 for permanent activity */
  int aid;	/* activity number */
  int rid;	/* csLib request id */

  enum genom_activity_status status;	/* current phase */
  int start, stop, pause;		/* status change request */
  genom_event state;			/* current FSM state */

  void *exdetail;			/* exception info, if any */
  const char *interruptedby;
};


/* === Service activity ==================================================== */


/* --- abort_activity --------------------------------------------------- */

/* activity data */
struct genom_bayes_opt_abort_activity_activity {
  struct genom_activity h;
  struct genom_bayes_opt_abort_activity_input in;
  struct genom_bayes_opt_abort_activity_output out;
};

/* input/output initializer */
static __inline__ void
genom_tinit_bayes_opt_abort_activity_activity(
  struct genom_bayes_opt_abort_activity_activity *a)
{
  genom_tinit_unsigned_long(
    &(a->in.activity));
}

/* input/output finalizer */
static __inline__ void
genom_tfini_bayes_opt_abort_activity_activity(
  struct genom_bayes_opt_abort_activity_activity *a)
{
  genom_tfini_unsigned_long(
    &(a->in.activity));
}


/* --- connect_port ----------------------------------------------------- */

/* activity data */
struct genom_bayes_opt_connect_port_activity {
  struct genom_activity h;
  struct genom_bayes_opt_connect_port_input in;
  struct genom_bayes_opt_connect_port_output out;
};

/* input/output initializer */
static __inline__ void
genom_tinit_bayes_opt_connect_port_activity(
  struct genom_bayes_opt_connect_port_activity *a)
{
  genom_tinit_string128(
    a->in.local);
  genom_tinit_string128(
    a->in.remote);
}

/* input/output finalizer */
static __inline__ void
genom_tfini_bayes_opt_connect_port_activity(
  struct genom_bayes_opt_connect_port_activity *a)
{
  genom_tfini_string128(
    a->in.local);
  genom_tfini_string128(
    a->in.remote);
}


/* --- connect_service -------------------------------------------------- */

/* activity data */
struct genom_bayes_opt_connect_service_activity {
  struct genom_activity h;
  struct genom_bayes_opt_connect_service_input in;
  struct genom_bayes_opt_connect_service_output out;
};

/* input/output initializer */
static __inline__ void
genom_tinit_bayes_opt_connect_service_activity(
  struct genom_bayes_opt_connect_service_activity *a)
{
  genom_tinit_string128(
    a->in.local);
  genom_tinit_string128(
    a->in.remote);
}

/* input/output finalizer */
static __inline__ void
genom_tfini_bayes_opt_connect_service_activity(
  struct genom_bayes_opt_connect_service_activity *a)
{
  genom_tfini_string128(
    a->in.local);
  genom_tfini_string128(
    a->in.remote);
}


/* --- kill ------------------------------------------------------------- */

/* activity data */
struct genom_bayes_opt_kill_activity {
  struct genom_activity h;
  struct genom_bayes_opt_kill_input in;
  struct genom_bayes_opt_kill_output out;
};

/* input/output initializer */
static __inline__ void
genom_tinit_bayes_opt_kill_activity(
  struct genom_bayes_opt_kill_activity *a)
{
  (void)a; /* fix -Wunused-parameter */
}

/* input/output finalizer */
static __inline__ void
genom_tfini_bayes_opt_kill_activity(
  struct genom_bayes_opt_kill_activity *a)
{
  (void)a; /* fix -Wunused-parameter */
}


/* --- initialize_optimizer --------------------------------------------- */

/* activity data */
struct genom_bayes_opt_initialize_optimizer_activity {
  struct genom_activity h;
  struct genom_bayes_opt_initialize_optimizer_input in;
  struct genom_bayes_opt_initialize_optimizer_output out;
};

/* input/output initializer */
static __inline__ void
genom_tinit_bayes_opt_initialize_optimizer_activity(
  struct genom_bayes_opt_initialize_optimizer_activity *a)
{
  (void)a; /* fix -Wunused-parameter */
}

/* input/output finalizer */
static __inline__ void
genom_tfini_bayes_opt_initialize_optimizer_activity(
  struct genom_bayes_opt_initialize_optimizer_activity *a)
{
  (void)a; /* fix -Wunused-parameter */
}


/* --- propose_parameters ----------------------------------------------- */

/* activity data */
struct genom_bayes_opt_propose_parameters_activity {
  struct genom_activity h;
  struct genom_bayes_opt_propose_parameters_input in;
  struct genom_bayes_opt_propose_parameters_output out;
};

/* input/output initializer */
static __inline__ void
genom_tinit_bayes_opt_propose_parameters_activity(
  struct genom_bayes_opt_propose_parameters_activity *a)
{
  genom_tinit_t_bayes_opt_suggestion(
    &(a->out.params));
}

/* input/output finalizer */
static __inline__ void
genom_tfini_bayes_opt_propose_parameters_activity(
  struct genom_bayes_opt_propose_parameters_activity *a)
{
  genom_tfini_t_bayes_opt_suggestion(
    &(a->out.params));
}


/* --- submit_result ---------------------------------------------------- */

/* activity data */
struct genom_bayes_opt_submit_result_activity {
  struct genom_activity h;
  struct genom_bayes_opt_submit_result_input in;
  struct genom_bayes_opt_submit_result_output out;
};

/* input/output initializer */
static __inline__ void
genom_tinit_bayes_opt_submit_result_activity(
  struct genom_bayes_opt_submit_result_activity *a)
{
  genom_tinit_double(
    &(a->in.score));
}

/* input/output finalizer */
static __inline__ void
genom_tfini_bayes_opt_submit_result_activity(
  struct genom_bayes_opt_submit_result_activity *a)
{
  genom_tfini_double(
    &(a->in.score));
}


/* --- get_best_parameters ---------------------------------------------- */

/* activity data */
struct genom_bayes_opt_get_best_parameters_activity {
  struct genom_activity h;
  struct genom_bayes_opt_get_best_parameters_input in;
  struct genom_bayes_opt_get_best_parameters_output out;
};

/* input/output initializer */
static __inline__ void
genom_tinit_bayes_opt_get_best_parameters_activity(
  struct genom_bayes_opt_get_best_parameters_activity *a)
{
  genom_tinit_t_bayes_opt_best(
    &(a->out.best_result));
}

/* input/output finalizer */
static __inline__ void
genom_tfini_bayes_opt_get_best_parameters_activity(
  struct genom_bayes_opt_get_best_parameters_activity *a)
{
  genom_tfini_t_bayes_opt_best(
    &(a->out.best_result));
}


/* --- reset_optimizer -------------------------------------------------- */

/* activity data */
struct genom_bayes_opt_reset_optimizer_activity {
  struct genom_activity h;
  struct genom_bayes_opt_reset_optimizer_input in;
  struct genom_bayes_opt_reset_optimizer_output out;
};

/* input/output initializer */
static __inline__ void
genom_tinit_bayes_opt_reset_optimizer_activity(
  struct genom_bayes_opt_reset_optimizer_activity *a)
{
  (void)a; /* fix -Wunused-parameter */
}

/* input/output finalizer */
static __inline__ void
genom_tfini_bayes_opt_reset_optimizer_activity(
  struct genom_bayes_opt_reset_optimizer_activity *a)
{
  (void)a; /* fix -Wunused-parameter */
}


/* --- Init ------------------------------------------------------------- */

/* activity data */
struct genom_bayes_opt_Init_activity {
  struct genom_activity h;
  struct genom_bayes_opt_Init_input in;
  struct genom_bayes_opt_Init_output out;
};

/* input/output initializer */
static __inline__ void
genom_tinit_bayes_opt_Init_activity(
  struct genom_bayes_opt_Init_activity *a)
{
  genom_tinit_array5_double(
    a->in.lower_bounds);
  genom_tinit_array5_double(
    a->in.upper_bounds);
  genom_tinit_long(
    &(a->in.max_iterations));
}

/* input/output finalizer */
static __inline__ void
genom_tfini_bayes_opt_Init_activity(
  struct genom_bayes_opt_Init_activity *a)
{
  genom_tfini_array5_double(
    a->in.lower_bounds);
  genom_tfini_array5_double(
    a->in.upper_bounds);
  genom_tfini_long(
    &(a->in.max_iterations));
}


/* --- AskNext ---------------------------------------------------------- */

/* activity data */
struct genom_bayes_opt_AskNext_activity {
  struct genom_activity h;
  struct genom_bayes_opt_AskNext_input in;
  struct genom_bayes_opt_AskNext_output out;
};

/* input/output initializer */
static __inline__ void
genom_tinit_bayes_opt_AskNext_activity(
  struct genom_bayes_opt_AskNext_activity *a)
{
  genom_tinit_t_bayes_opt_suggestion(
    &(a->out.params_out));
}

/* input/output finalizer */
static __inline__ void
genom_tfini_bayes_opt_AskNext_activity(
  struct genom_bayes_opt_AskNext_activity *a)
{
  genom_tfini_t_bayes_opt_suggestion(
    &(a->out.params_out));
}


/* --- SubmitResult ----------------------------------------------------- */

/* activity data */
struct genom_bayes_opt_SubmitResult_activity {
  struct genom_activity h;
  struct genom_bayes_opt_SubmitResult_input in;
  struct genom_bayes_opt_SubmitResult_output out;
};

/* input/output initializer */
static __inline__ void
genom_tinit_bayes_opt_SubmitResult_activity(
  struct genom_bayes_opt_SubmitResult_activity *a)
{
  genom_tinit_double(
    &(a->in.score));
}

/* input/output finalizer */
static __inline__ void
genom_tfini_bayes_opt_SubmitResult_activity(
  struct genom_bayes_opt_SubmitResult_activity *a)
{
  genom_tfini_double(
    &(a->in.score));
}


/* --- GetBest ---------------------------------------------------------- */

/* activity data */
struct genom_bayes_opt_GetBest_activity {
  struct genom_activity h;
  struct genom_bayes_opt_GetBest_input in;
  struct genom_bayes_opt_GetBest_output out;
};

/* input/output initializer */
static __inline__ void
genom_tinit_bayes_opt_GetBest_activity(
  struct genom_bayes_opt_GetBest_activity *a)
{
  genom_tinit_t_bayes_opt_best(
    &(a->out.best_result_out));
}

/* input/output finalizer */
static __inline__ void
genom_tfini_bayes_opt_GetBest_activity(
  struct genom_bayes_opt_GetBest_activity *a)
{
  genom_tfini_t_bayes_opt_best(
    &(a->out.best_result_out));
}



/* === Activities ========================================================== */

union genom_any_activity {
  struct genom_activity any;
  struct genom_bayes_opt_abort_activity_activity s_abort_activity;
  struct genom_bayes_opt_connect_port_activity s_connect_port;
  struct genom_bayes_opt_connect_service_activity s_connect_service;
  struct genom_bayes_opt_kill_activity s_kill;
  struct genom_bayes_opt_initialize_optimizer_activity s_initialize_optimizer;
  struct genom_bayes_opt_propose_parameters_activity s_propose_parameters;
  struct genom_bayes_opt_submit_result_activity s_submit_result;
  struct genom_bayes_opt_get_best_parameters_activity s_get_best_parameters;
  struct genom_bayes_opt_reset_optimizer_activity s_reset_optimizer;
  struct genom_bayes_opt_Init_activity s_Init;
  struct genom_bayes_opt_AskNext_activity s_AskNext;
  struct genom_bayes_opt_SubmitResult_activity s_SubmitResult;
  struct genom_bayes_opt_GetBest_activity s_GetBest;
};

struct genom_activities {
  union genom_any_activity a[16];
};

static inline size_t
genom_max_activities(void) {
  struct genom_activities *a;
  return sizeof(a->a)/sizeof(a->a[0]);
}

int	genom_bayes_opt_activity_alloc(struct genom_activities *activities);
void	genom_bayes_opt_activity_report(struct genom_component_data *self,
		struct genom_activity *a);
int	genom_bayes_opt_activity_encodex(char *buffer, int size, char *dst,
                int maxsize);
int	genom_bayes_opt_interrupt_reqd(struct genom_component_data *self,
		struct genom_activity *a);

#endif /* H_BAYES_OPT_ACTIVITY */
