/* --- Generated by genom 3.1. Do not edit ------------------------------ */

#include "autoconf/acheader.h"

#include <signal.h>
#include <unistd.h>

#include "bayes_opt_c_types.h"
#include "prof.h"
#include "typecopy.h"
#include "bayes_opt_parameters.h"
#include "bayes_opt_control_task.h"


/* --- genom_abort_activity_codel ------------------------------------------ */

genom_event
genom_abort_activity_codel(uint32_t activity, genom_context ctx)
{
  if (activity & 0x80000000UL)
    return genom_no_such_activity(ctx);

  /* task optimize */
  {
    struct genom_component_data *self = ctx->data->self;
    struct genom_activity *a;
    size_t id;

    pthread_mutex_lock(&self->tasks.optimize.lock);
    for(id = 0; id < genom_max_activities(); id++) {
      a = &self->tasks.optimize.activities.a[id].any;
      switch(a->status) {
        case ACT_VOID: continue;

        case ACT_INIT: case ACT_RUN:
          if (a->aid != (int32_t)activity) continue;
          a->stop = 1;
          a->interruptedby = "abort_activity";
          pthread_mutex_unlock(&self->tasks.optimize.lock);
          return genom_ok;

        case ACT_STOP: case ACT_ETHER:
          if (a->aid != (int32_t)activity) continue;
          pthread_mutex_unlock(&self->tasks.optimize.lock);
          return genom_ok;
      }
    }
    pthread_mutex_unlock(&self->tasks.optimize.lock);
  }

  return genom_no_such_activity(ctx);
}


/* --- genom_connect_port_codel -------------------------------------------- */

genom_event
genom_connect_port_codel(const char local[128], const char remote[128],
                         genom_context ctx)
{
  struct genom_component_data *self = ctx->data->self;
  genom_event s;
  (void)local; (void)remote; /* fix -Wunused-parameter */

  genom_take_resource(
    self,
    self->resources.task_optimize ||
    self->resources.all,

    self->resources.all = 1);

  /* find the inport */
  if (!strcmp(local, "result")) {
    s = genom_bayes_opt_result_connect(remote, ctx);
    goto done;
  }
  if (!strcmp(local, "allow")) {
    s = genom_bayes_opt_allow_connect(remote, ctx);
    goto done;
  }

  s = genom_no_such_inport(ctx);
  goto done;

done:
  genom_give_resource(self, self->resources.all = 0);
  return s;
}


/* --- genom_connect_remote_codel ------------------------------------------ */

genom_event
genom_connect_remote_codel(const char local[128], const char remote[128],
                           genom_context ctx)
{
  struct genom_component_data *self = ctx->data->self;
  genom_event s;
  (void)local; (void)remote; /* fix -Wunused-parameter */

  genom_take_resource(
    self,
    self->resources.task_optimize ||
    self->resources.all,

    self->resources.all = 1);

  /* find the remote */

  s = genom_no_such_remote(ctx);
  goto done;

done:
  genom_give_resource(self, self->resources.all = 0);
  return s;
}


/* --- genom_kill_codel ---------------------------------------------------- */

genom_event
genom_kill_codel(genom_context self)
{
  (void)self; /* fix -Wunused-parameter */
  kill(getpid(), SIGTERM);
  return genom_ok;
}


/* --- Service abort_activity control codels ---------------------------- */

genom_event
genom_bayes_opt_abort_activity_controlcb(
  struct genom_component_data *self,
  struct genom_bayes_opt_abort_activity_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */
  genom_prof_decl(event);

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */
  genom_prof_enter(&event);
  genom_take_resource(
    self,
    self->resources.all,

    self->resources.control = genom_abort_activity_codel);
  genom_prof_start(&event);
  s = genom_abort_activity_codel(a->in.activity, &self->control.context);
  genom_give_resource(self, self->resources.control = NULL);

  genom_prof_leave(&event);
  genom_prof_collect(
    &event, genom_instance,
    "<control>", "abort_activity", "genom_abort_activity_codel", "codel", s);

  genom_log_debug("service %s codel %s returned %s",
                  "abort_activity", "genom_abort_activity_codel", s?s:"ok");
  if (s
      && s != genom_incompatible_digest_id
      && s != genom_bad_transition_id
      && s != genom_interrupted_id
      && s != genom_serialization_id
      && s != genom_too_many_activities_id
      && s != genom_disallowed_id
      && s != genom_mwerr_id
      && s != genom_no_such_activity_id
      ) {
    genom_unkex_detail d;
    strncpy(d.what, s, sizeof(d.what)); d.what[sizeof(d.what)-1] = *"";
    genom_log_warn(
      0, "unknown exception %s for codel %s in service %s",
      s, "genom_abort_activity_codel", "abort_activity");
    s = genom_unkex(&d, &self->control.context);
  }
  if (s) return s;

  return s;
}


/* --- Service connect_port control codels ------------------------------ */

genom_event
genom_bayes_opt_connect_port_controlcb(
  struct genom_component_data *self,
  struct genom_bayes_opt_connect_port_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */
  genom_prof_decl(event);

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */
  genom_prof_enter(&event);
  genom_take_resource(
    self,
    self->resources.all,

    self->resources.control = genom_connect_port_codel);
  genom_prof_start(&event);
  s = genom_connect_port_codel(a->in.local, a->in.remote, &self->control.context);
  genom_give_resource(self, self->resources.control = NULL);

  genom_prof_leave(&event);
  genom_prof_collect(
    &event, genom_instance,
    "<control>", "connect_port", "genom_connect_port_codel", "codel", s);

  genom_log_debug("service %s codel %s returned %s",
                  "connect_port", "genom_connect_port_codel", s?s:"ok");
  if (s
      && s != genom_incompatible_digest_id
      && s != genom_bad_transition_id
      && s != genom_interrupted_id
      && s != genom_serialization_id
      && s != genom_too_many_activities_id
      && s != genom_disallowed_id
      && s != genom_mwerr_id
      && s != genom_no_such_inport_id
      && s != genom_no_such_outport_id
      && s != genom_port_io_id
      ) {
    genom_unkex_detail d;
    strncpy(d.what, s, sizeof(d.what)); d.what[sizeof(d.what)-1] = *"";
    genom_log_warn(
      0, "unknown exception %s for codel %s in service %s",
      s, "genom_connect_port_codel", "connect_port");
    s = genom_unkex(&d, &self->control.context);
  }
  if (s) return s;

  return s;
}


/* --- Service connect_service control codels --------------------------- */

genom_event
genom_bayes_opt_connect_service_controlcb(
  struct genom_component_data *self,
  struct genom_bayes_opt_connect_service_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */
  genom_prof_decl(event);

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */
  genom_prof_enter(&event);
  genom_take_resource(
    self,
    self->resources.all,

    self->resources.control = genom_connect_remote_codel);
  genom_prof_start(&event);
  s = genom_connect_remote_codel(a->in.local, a->in.remote, &self->control.context);
  genom_give_resource(self, self->resources.control = NULL);

  genom_prof_leave(&event);
  genom_prof_collect(
    &event, genom_instance,
    "<control>", "connect_service", "genom_connect_remote_codel", "codel", s);

  genom_log_debug("service %s codel %s returned %s",
                  "connect_service", "genom_connect_remote_codel", s?s:"ok");
  if (s
      && s != genom_incompatible_digest_id
      && s != genom_bad_transition_id
      && s != genom_interrupted_id
      && s != genom_serialization_id
      && s != genom_too_many_activities_id
      && s != genom_disallowed_id
      && s != genom_mwerr_id
      && s != genom_no_such_remote_id
      && s != genom_no_such_service_id
      && s != genom_remote_io_id
      ) {
    genom_unkex_detail d;
    strncpy(d.what, s, sizeof(d.what)); d.what[sizeof(d.what)-1] = *"";
    genom_log_warn(
      0, "unknown exception %s for codel %s in service %s",
      s, "genom_connect_remote_codel", "connect_service");
    s = genom_unkex(&d, &self->control.context);
  }
  if (s) return s;

  return s;
}


/* --- Service kill control codels -------------------------------------- */

genom_event
genom_bayes_opt_kill_controlcb(
  struct genom_component_data *self,
  struct genom_bayes_opt_kill_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */
  genom_prof_decl(event);

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */
  genom_prof_enter(&event);
  genom_take_resource(
    self,
    self->resources.all,

    self->resources.control = genom_kill_codel);
  genom_prof_start(&event);
  s = genom_kill_codel(&self->control.context);
  genom_give_resource(self, self->resources.control = NULL);

  genom_prof_leave(&event);
  genom_prof_collect(
    &event, genom_instance,
    "<control>", "kill", "genom_kill_codel", "codel", s);

  genom_log_debug("service %s codel %s returned %s",
                  "kill", "genom_kill_codel", s?s:"ok");
  if (s
      && s != genom_incompatible_digest_id
      && s != genom_bad_transition_id
      && s != genom_interrupted_id
      && s != genom_serialization_id
      && s != genom_too_many_activities_id
      && s != genom_disallowed_id
      && s != genom_mwerr_id
      ) {
    genom_unkex_detail d;
    strncpy(d.what, s, sizeof(d.what)); d.what[sizeof(d.what)-1] = *"";
    genom_log_warn(
      0, "unknown exception %s for codel %s in service %s",
      s, "genom_kill_codel", "kill");
    s = genom_unkex(&d, &self->control.context);
  }
  if (s) return s;

  return s;
}


/* --- Service initialize_optimizer control codels ---------------------- */

genom_event
genom_bayes_opt_initialize_optimizer_controlcb(
  struct genom_component_data *self,
  struct genom_bayes_opt_initialize_optimizer_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */

  return s;
}


/* --- Service propose_parameters control codels ------------------------ */

genom_event
genom_bayes_opt_propose_parameters_controlcb(
  struct genom_component_data *self,
  struct genom_bayes_opt_propose_parameters_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */

  return s;
}


/* --- Service submit_result control codels ----------------------------- */

genom_event
genom_bayes_opt_submit_result_controlcb(
  struct genom_component_data *self,
  struct genom_bayes_opt_submit_result_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */

  return s;
}


/* --- Service get_best_parameters control codels ----------------------- */

genom_event
genom_bayes_opt_get_best_parameters_controlcb(
  struct genom_component_data *self,
  struct genom_bayes_opt_get_best_parameters_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */

  return s;
}


/* --- Service reset_optimizer control codels --------------------------- */

genom_event
genom_bayes_opt_reset_optimizer_controlcb(
  struct genom_component_data *self,
  struct genom_bayes_opt_reset_optimizer_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */

  return s;
}


/* --- Service Init control codels -------------------------------------- */

genom_event
genom_bayes_opt_Init_controlcb(
  struct genom_component_data *self,
  struct genom_bayes_opt_Init_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */

  return s;
}


/* --- Service AskNext control codels ----------------------------------- */

genom_event
genom_bayes_opt_AskNext_controlcb(
  struct genom_component_data *self,
  struct genom_bayes_opt_AskNext_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */
  genom_prof_decl(event);

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */
  genom_prof_enter(&event);
  genom_take_resource(
    self,
    self->resources.task_optimize == init ||
    self->resources.task_optimize == wait_result ||
    self->resources.task_optimize == compute ||
    self->resources.task_optimize == publish ||
    self->resources.task_optimize == failed ||
    self->resources.task_optimize == boProposeParams ||
    self->resources.task_optimize == boUpdateOptimizer ||
    self->resources.all,

    self->resources.control = checkInitialized);
  genom_prof_start(&event);
  s = checkInitialized(&(self->ids.state), &self->control.context);
  genom_give_resource(self, self->resources.control = NULL);
  genom_prof_leave(&event);

  genom_prof_collect(
    &event, genom_instance,
    "<control>", "AskNext", "checkInitialized", "validate", s);
  genom_log_debug("service AskNext validation returned %s", s?s:"ok");
  if (s) return s;

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */

  return s;
}


/* --- Service SubmitResult control codels ------------------------------ */

genom_event
genom_bayes_opt_SubmitResult_controlcb(
  struct genom_component_data *self,
  struct genom_bayes_opt_SubmitResult_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */
  genom_prof_decl(event);

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */
  genom_prof_enter(&event);
  genom_take_resource(
    self,
    self->resources.task_optimize == init ||
    self->resources.task_optimize == wait_result ||
    self->resources.task_optimize == compute ||
    self->resources.task_optimize == publish ||
    self->resources.task_optimize == failed ||
    self->resources.task_optimize == boProposeParams ||
    self->resources.task_optimize == boUpdateOptimizer ||
    self->resources.all,

    self->resources.control = checkInitialized);
  genom_prof_start(&event);
  s = checkInitialized(&(self->ids.state), &self->control.context);
  genom_give_resource(self, self->resources.control = NULL);
  genom_prof_leave(&event);

  genom_prof_collect(
    &event, genom_instance,
    "<control>", "SubmitResult", "checkInitialized", "validate", s);
  genom_log_debug("service SubmitResult validation returned %s", s?s:"ok");
  if (s) return s;

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */

  return s;
}


/* --- Service GetBest control codels ----------------------------------- */

genom_event
genom_bayes_opt_GetBest_controlcb(
  struct genom_component_data *self,
  struct genom_bayes_opt_GetBest_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */

  return s;
}

/* eof */
