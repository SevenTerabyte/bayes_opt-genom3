/* --- Generated by genom 3.1. Do not edit ------------------------------ */

#include "autoconf/acheader.h"

#include <assert.h>
#include <stddef.h>

#include "portLib.h"
#include "errnoLib.h"
#include "gcomLib.h"

#include "bayes_opt_parameters.h"
#include "bayes_opt_msglib.h"


/* --- Service abort_activity encoding/decoding ------------------------- */

int
genom_bayes_opt_abort_activity_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_bayes_opt_abort_activity_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  ssize_t len = size;
  char *p = buffer;
  struct genom_bayes_opt_abort_activity_input *in =
    (struct genom_bayes_opt_abort_activity_input *)dst;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = genom_deserialize_unsigned_long(
    &p, &len, &(in->activity));
  if (s) {
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
    }
    return ERROR;
  }

  if (len != 0) {
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}


/* --- Service connect_port encoding/decoding --------------------------- */

int
genom_bayes_opt_connect_port_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_bayes_opt_connect_port_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  ssize_t len = size;
  char *p = buffer;
  struct genom_bayes_opt_connect_port_input *in =
    (struct genom_bayes_opt_connect_port_input *)dst;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = genom_deserialize_string128(
    &p, &len, in->local);
  if (s) {
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
    }
    return ERROR;
  }
  s = genom_deserialize_string128(
    &p, &len, in->remote);
  if (s) {
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
    }
    return ERROR;
  }

  if (len != 0) {
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}


/* --- Service connect_service encoding/decoding ------------------------ */

int
genom_bayes_opt_connect_service_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_bayes_opt_connect_service_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  ssize_t len = size;
  char *p = buffer;
  struct genom_bayes_opt_connect_service_input *in =
    (struct genom_bayes_opt_connect_service_input *)dst;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = genom_deserialize_string128(
    &p, &len, in->local);
  if (s) {
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
    }
    return ERROR;
  }
  s = genom_deserialize_string128(
    &p, &len, in->remote);
  if (s) {
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
    }
    return ERROR;
  }

  if (len != 0) {
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}


/* --- Service kill encoding/decoding ----------------------------------- */

int
genom_bayes_opt_kill_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_bayes_opt_kill_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}


/* --- Service reset_optimizer encoding/decoding ------------------------ */

int
genom_bayes_opt_reset_optimizer_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_bayes_opt_reset_optimizer_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}


/* --- Service Init encoding/decoding ----------------------------------- */

int
genom_bayes_opt_Init_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_bayes_opt_Init_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  ssize_t len = size;
  char *p = buffer;
  struct genom_bayes_opt_Init_input *in =
    (struct genom_bayes_opt_Init_input *)dst;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = genom_deserialize_array5_double(
    &p, &len, in->lower_bounds);
  if (s) {
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
    }
    return ERROR;
  }
  s = genom_deserialize_array5_double(
    &p, &len, in->upper_bounds);
  if (s) {
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
    }
    return ERROR;
  }
  s = genom_deserialize_long(
    &p, &len, &(in->max_iterations));
  if (s) {
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
    }
    return ERROR;
  }
  s = genom_deserialize_double(
    &p, &len, &(in->reference_x));
  if (s) {
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
    }
    return ERROR;
  }
  s = genom_deserialize_double(
    &p, &len, &(in->reference_y));
  if (s) {
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
    }
    return ERROR;
  }
  s = genom_deserialize_double(
    &p, &len, &(in->reference_z));
  if (s) {
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
    }
    return ERROR;
  }
  s = genom_deserialize_double(
    &p, &len, &(in->reference_qw));
  if (s) {
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
    }
    return ERROR;
  }
  s = genom_deserialize_double(
    &p, &len, &(in->reference_qx));
  if (s) {
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
    }
    return ERROR;
  }
  s = genom_deserialize_double(
    &p, &len, &(in->reference_qy));
  if (s) {
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
    }
    return ERROR;
  }
  s = genom_deserialize_double(
    &p, &len, &(in->reference_qz));
  if (s) {
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR); break;
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED); break;
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
    }
    return ERROR;
  }

  if (len != 0) {
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}


/* --- Service AskNext encoding/decoding -------------------------------- */

int
genom_bayes_opt_AskNext_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  char *p = dst;
  struct genom_bayes_opt_AskNext_output *out =
    (struct genom_bayes_opt_AskNext_output *)buffer;
  ssize_t s;
  (void)size; /* fix -Wunused-parameter */

  s = 1;
  s += genom_serialen_t_bayes_opt_suggestion(
    &(out->params_out));
  if (s > maxsize) return ERROR;

  *p++ = *"";
  genom_serialize_t_bayes_opt_suggestion(
    &p, &(out->params_out));

  return s;
}

int
genom_bayes_opt_AskNext_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}


/* --- Service UpdateFromMeasure encoding/decoding ---------------------- */

int
genom_bayes_opt_UpdateFromMeasure_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_bayes_opt_UpdateFromMeasure_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}


/* --- Service GetBest encoding/decoding -------------------------------- */

int
genom_bayes_opt_GetBest_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  char *p = dst;
  struct genom_bayes_opt_GetBest_output *out =
    (struct genom_bayes_opt_GetBest_output *)buffer;
  ssize_t s;
  (void)size; /* fix -Wunused-parameter */

  s = 1;
  s += genom_serialen_t_bayes_opt_best(
    &(out->best_result_out));
  if (s > maxsize) return ERROR;

  *p++ = *"";
  genom_serialize_t_bayes_opt_best(
    &p, &(out->best_result_out));

  return s;
}

int
genom_bayes_opt_GetBest_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}


/* --- Service Reset encoding/decoding ---------------------------------- */

int
genom_bayes_opt_Reset_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_bayes_opt_Reset_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}



/* --- Exception incompatible_digest encoding --------------------------- */

int
genom_bayes_opt_genom_incompatible_digest_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_incompatible_digest_detail *out = (genom_incompatible_digest_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_incompatible_digest_id);
  s += genom_serialen_t_genom_incompatible_digest(out);
  if (s > maxsize) return ERROR;

  memcpy(p, genom_incompatible_digest_id, sizeof(genom_incompatible_digest_id));
  p += sizeof(genom_incompatible_digest_id);
  genom_serialize_t_genom_incompatible_digest(&p, out);
  return s;
}


/* --- Exception bad_transition encoding -------------------------------- */

int
genom_bayes_opt_genom_bad_transition_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_bad_transition_detail *out = (genom_bad_transition_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_bad_transition_id);
  s += genom_serialen_t_genom_bad_transition(out);
  if (s > maxsize) return ERROR;

  memcpy(p, genom_bad_transition_id, sizeof(genom_bad_transition_id));
  p += sizeof(genom_bad_transition_id);
  genom_serialize_t_genom_bad_transition(&p, out);
  return s;
}


/* --- Exception interrupted encoding ----------------------------------- */

int
genom_bayes_opt_genom_interrupted_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_interrupted_detail *out = (genom_interrupted_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_interrupted_id);
  s += genom_serialen_t_genom_interrupted(out);
  if (s > maxsize) return ERROR;

  memcpy(p, genom_interrupted_id, sizeof(genom_interrupted_id));
  p += sizeof(genom_interrupted_id);
  genom_serialize_t_genom_interrupted(&p, out);
  return s;
}


/* --- Exception serialization encoding --------------------------------- */

int
genom_bayes_opt_genom_serialization_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_serialization_detail *out = (genom_serialization_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_serialization_id);
  s += genom_serialen_t_genom_serialization(out);
  if (s > maxsize) return ERROR;

  memcpy(p, genom_serialization_id, sizeof(genom_serialization_id));
  p += sizeof(genom_serialization_id);
  genom_serialize_t_genom_serialization(&p, out);
  return s;
}


/* --- Exception too_many_activities encoding --------------------------- */

int
genom_bayes_opt_genom_too_many_activities_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_too_many_activities_detail *out = (genom_too_many_activities_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_too_many_activities_id);
  s += genom_serialen_t_genom_too_many_activities(out);
  if (s > maxsize) return ERROR;

  memcpy(p, genom_too_many_activities_id, sizeof(genom_too_many_activities_id));
  p += sizeof(genom_too_many_activities_id);
  genom_serialize_t_genom_too_many_activities(&p, out);
  return s;
}


/* --- Exception disallowed encoding ------------------------------------ */

int
genom_bayes_opt_genom_disallowed_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_disallowed_detail *out = (genom_disallowed_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_disallowed_id);
  s += genom_serialen_t_genom_disallowed(out);
  if (s > maxsize) return ERROR;

  memcpy(p, genom_disallowed_id, sizeof(genom_disallowed_id));
  p += sizeof(genom_disallowed_id);
  genom_serialize_t_genom_disallowed(&p, out);
  return s;
}


/* --- Exception mwerr encoding ----------------------------------------- */

int
genom_bayes_opt_genom_mwerr_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_mwerr_detail *out = (genom_mwerr_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_mwerr_id);
  s += genom_serialen_t_genom_mwerr(out);
  if (s > maxsize) return ERROR;

  memcpy(p, genom_mwerr_id, sizeof(genom_mwerr_id));
  p += sizeof(genom_mwerr_id);
  genom_serialize_t_genom_mwerr(&p, out);
  return s;
}


/* --- Exception no_such_activity encoding ------------------------------ */

int
genom_bayes_opt_genom_no_such_activity_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_no_such_activity_detail *out = (genom_no_such_activity_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_no_such_activity_id);
  s += genom_serialen_t_genom_no_such_activity(out);
  if (s > maxsize) return ERROR;

  memcpy(p, genom_no_such_activity_id, sizeof(genom_no_such_activity_id));
  p += sizeof(genom_no_such_activity_id);
  genom_serialize_t_genom_no_such_activity(&p, out);
  return s;
}


/* --- Exception no_such_inport encoding -------------------------------- */

int
genom_bayes_opt_genom_no_such_inport_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_no_such_inport_detail *out = (genom_no_such_inport_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_no_such_inport_id);
  s += genom_serialen_t_genom_no_such_inport(out);
  if (s > maxsize) return ERROR;

  memcpy(p, genom_no_such_inport_id, sizeof(genom_no_such_inport_id));
  p += sizeof(genom_no_such_inport_id);
  genom_serialize_t_genom_no_such_inport(&p, out);
  return s;
}


/* --- Exception no_such_outport encoding ------------------------------- */

int
genom_bayes_opt_genom_no_such_outport_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_no_such_outport_detail *out = (genom_no_such_outport_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_no_such_outport_id);
  s += genom_serialen_t_genom_no_such_outport(out);
  if (s > maxsize) return ERROR;

  memcpy(p, genom_no_such_outport_id, sizeof(genom_no_such_outport_id));
  p += sizeof(genom_no_such_outport_id);
  genom_serialize_t_genom_no_such_outport(&p, out);
  return s;
}


/* --- Exception port_io encoding --------------------------------------- */

int
genom_bayes_opt_genom_port_io_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_port_io_detail *out = (genom_port_io_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_port_io_id);
  s += genom_serialen_t_genom_port_io(out);
  if (s > maxsize) return ERROR;

  memcpy(p, genom_port_io_id, sizeof(genom_port_io_id));
  p += sizeof(genom_port_io_id);
  genom_serialize_t_genom_port_io(&p, out);
  return s;
}


/* --- Exception no_such_remote encoding -------------------------------- */

int
genom_bayes_opt_genom_no_such_remote_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_no_such_remote_detail *out = (genom_no_such_remote_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_no_such_remote_id);
  s += genom_serialen_t_genom_no_such_remote(out);
  if (s > maxsize) return ERROR;

  memcpy(p, genom_no_such_remote_id, sizeof(genom_no_such_remote_id));
  p += sizeof(genom_no_such_remote_id);
  genom_serialize_t_genom_no_such_remote(&p, out);
  return s;
}


/* --- Exception no_such_service encoding ------------------------------- */

int
genom_bayes_opt_genom_no_such_service_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_no_such_service_detail *out = (genom_no_such_service_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_no_such_service_id);
  s += genom_serialen_t_genom_no_such_service(out);
  if (s > maxsize) return ERROR;

  memcpy(p, genom_no_such_service_id, sizeof(genom_no_such_service_id));
  p += sizeof(genom_no_such_service_id);
  genom_serialize_t_genom_no_such_service(&p, out);
  return s;
}


/* --- Exception remote_io encoding ------------------------------------- */

int
genom_bayes_opt_genom_remote_io_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_remote_io_detail *out = (genom_remote_io_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_remote_io_id);
  s += genom_serialen_t_genom_remote_io(out);
  if (s > maxsize) return ERROR;

  memcpy(p, genom_remote_io_id, sizeof(genom_remote_io_id));
  p += sizeof(genom_remote_io_id);
  genom_serialize_t_genom_remote_io(&p, out);
  return s;
}


/* --- Exception INVALID_BOUNDS encoding -------------------------------- */

int
genom_bayes_opt_bayes_opt_INVALID_BOUNDS_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  bayes_opt_INVALID_BOUNDS_detail *out = (bayes_opt_INVALID_BOUNDS_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(bayes_opt_INVALID_BOUNDS_id);
  s += genom_serialen_t_bayes_opt_INVALID_BOUNDS(out);
  if (s > maxsize) return ERROR;

  memcpy(p, bayes_opt_INVALID_BOUNDS_id, sizeof(bayes_opt_INVALID_BOUNDS_id));
  p += sizeof(bayes_opt_INVALID_BOUNDS_id);
  genom_serialize_t_bayes_opt_INVALID_BOUNDS(&p, out);
  return s;
}


/* --- Exception e_sys encoding ----------------------------------------- */

int
genom_bayes_opt_bayes_opt_e_sys_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  bayes_opt_e_sys_detail *out = (bayes_opt_e_sys_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(bayes_opt_e_sys_id);
  s += genom_serialen_t_bayes_opt_e_sys(out);
  if (s > maxsize) return ERROR;

  memcpy(p, bayes_opt_e_sys_id, sizeof(bayes_opt_e_sys_id));
  p += sizeof(bayes_opt_e_sys_id);
  genom_serialize_t_bayes_opt_e_sys(&p, out);
  return s;
}


/* --- Exception NOT_INITIALIZED encoding ------------------------------- */

int
genom_bayes_opt_bayes_opt_NOT_INITIALIZED_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  bayes_opt_NOT_INITIALIZED_detail *out = (bayes_opt_NOT_INITIALIZED_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(bayes_opt_NOT_INITIALIZED_id);
  s += genom_serialen_t_bayes_opt_NOT_INITIALIZED(out);
  if (s > maxsize) return ERROR;

  memcpy(p, bayes_opt_NOT_INITIALIZED_id, sizeof(bayes_opt_NOT_INITIALIZED_id));
  p += sizeof(bayes_opt_NOT_INITIALIZED_id);
  genom_serialize_t_bayes_opt_NOT_INITIALIZED(&p, out);
  return s;
}


/* --- Exception OPTIMIZATION_FAILED encoding --------------------------- */

int
genom_bayes_opt_bayes_opt_OPTIMIZATION_FAILED_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  bayes_opt_OPTIMIZATION_FAILED_detail *out = (bayes_opt_OPTIMIZATION_FAILED_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(bayes_opt_OPTIMIZATION_FAILED_id);
  s += genom_serialen_t_bayes_opt_OPTIMIZATION_FAILED(out);
  if (s > maxsize) return ERROR;

  memcpy(p, bayes_opt_OPTIMIZATION_FAILED_id, sizeof(bayes_opt_OPTIMIZATION_FAILED_id));
  p += sizeof(bayes_opt_OPTIMIZATION_FAILED_id);
  genom_serialize_t_bayes_opt_OPTIMIZATION_FAILED(&p, out);
  return s;
}


/* --- Exception NO_MEASUREMENT encoding -------------------------------- */

int
genom_bayes_opt_bayes_opt_NO_MEASUREMENT_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  bayes_opt_NO_MEASUREMENT_detail *out = (bayes_opt_NO_MEASUREMENT_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(bayes_opt_NO_MEASUREMENT_id);
  s += genom_serialen_t_bayes_opt_NO_MEASUREMENT(out);
  if (s > maxsize) return ERROR;

  memcpy(p, bayes_opt_NO_MEASUREMENT_id, sizeof(bayes_opt_NO_MEASUREMENT_id));
  p += sizeof(bayes_opt_NO_MEASUREMENT_id);
  genom_serialize_t_bayes_opt_NO_MEASUREMENT(&p, out);
  return s;
}


/* --- Exception NO_BEST_RESULT encoding -------------------------------- */

int
genom_bayes_opt_bayes_opt_NO_BEST_RESULT_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  bayes_opt_NO_BEST_RESULT_detail *out = (bayes_opt_NO_BEST_RESULT_detail *)buffer;
  char *p = dst;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(bayes_opt_NO_BEST_RESULT_id);
  s += genom_serialen_t_bayes_opt_NO_BEST_RESULT(out);
  if (s > maxsize) return ERROR;

  memcpy(p, bayes_opt_NO_BEST_RESULT_id, sizeof(bayes_opt_NO_BEST_RESULT_id));
  p += sizeof(bayes_opt_NO_BEST_RESULT_id);
  genom_serialize_t_bayes_opt_NO_BEST_RESULT(&p, out);
  return s;
}


/* --- Exception genom::unkex encoding ------------------------------------- */

int
genom_bayes_opt_genom_unkex_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_unkex_detail *out = (genom_unkex_detail *)buffer;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_unkex_id);
  s += sizeof(*out);
  if (s > maxsize) return ERROR;

  memcpy(dst, genom_unkex_id, sizeof(genom_unkex_id));
  dst += sizeof(genom_unkex_id);
  memcpy(dst, out, sizeof(*out));
  return s;
}


/* --- Exception genom::syserr encoding ------------------------------------ */

int
genom_bayes_opt_genom_syserr_encodex(
  char *buffer, int size, char *dst, int maxsize)
{
  genom_syserr_detail *out = (genom_syserr_detail *)buffer;
  ssize_t s;
  (void)size; /* fix -Wunused-parameters */

  s = sizeof(genom_syserr_id);
  s += sizeof(*out);
  if (s > maxsize) return ERROR;

  memcpy(dst, genom_syserr_id, sizeof(genom_syserr_id));
  dst += sizeof(genom_syserr_id);
  memcpy(dst, out, sizeof(*out));
  return s;
}
